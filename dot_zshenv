# Keep this Home-level file as the minimal bootstrap zsh needs in order to
# discover the remaining startup files under XDG_CONFIG_HOME.
export ZDOTDIR="$HOME/.config/zsh"
[[ -r "$ZDOTDIR/.zshenv" ]] && source "$ZDOTDIR/.zshenv"
